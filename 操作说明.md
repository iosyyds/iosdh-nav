# 甜甜导航 - 操作说明

## 项目简介
基于 Next.js 的 iOS 资源导航站，数据存储在 **jsonbin.io** 免费云端，支持后台管理。

---

## 一、首次部署（5 分钟）

### 1. 安装依赖并构建
```bash
npm install
npm run build
```

### 2. 本地预览
```bash
npx serve out -p 3000
```
打开 http://localhost:3000 查看效果。

### 3. 部署到 GitHub Pages（推荐）
1. 将项目推送到 GitHub 仓库
2. 仓库 Settings → Pages → Source 选「GitHub Actions」
3. 项目已包含 `.github/workflows/deploy.yml`，push 后自动部署

---

## 二、注册 jsonbin.io 并获取 API Key（后台云端数据必备）

### 1. 注册账号
- 打开 https://jsonbin.io/login
- 用 GitHub / Google 账号登录（免费）

### 2. 获取 API Key
- 登录后进入 Dashboard：https://jsonbin.io/app/dashboard
- 找到 **API Keys** 区域
- 复制你的 **X-Master-Key**（格式类似 `$2b$10$xxxxxxxxxxxx`）
- ⚠️ 这个 Key 只显示一次，请妥善保存

### 3. 免费额度
- 10000 请求/月，个人导航站完全够用

---

## 三、后台首次登录（自动创建 Bin）

### 方式 A：首次使用（自动创建 Bin，推荐）
1. 打开网站 `/admin` 页面
2. 切换到「首次使用」标签
3. 输入：
   - **管理密码**：默认 `ttnav2026`（可在 `lib/adminConfig.ts` 中修改）
   - **jsonbin API Key**：刚才复制的 X-Master-Key
4. 点击「创建 Bin 并登录」
5. 系统会自动在你的 jsonbin 账号中创建一个 Bin，并把 400 个站点数据上传
6. 创建成功后自动登录，Bin ID 会保存在本地浏览器

### 方式 B：已有 Bin（换浏览器/换设备时用）
1. 打开 `/admin`
2. 保持在「已有 Bin」标签
3. 输入：
   - 管理密码
   - API Key
   - Bin ID（在 jsonbin Dashboard 的 Bins 列表中复制，格式类似 `65f1a2b3c4d5e6f7a8b9c0d1`）
4. 点击「登录后台」

---

## 四、后台使用流程

### 1. 站点管理
- 左侧选择分类，右侧显示该分类下的站点
- 点击「+ 新增站点」添加新站点
- 点击「编辑」修改站点信息
- 点击「删除」移除站点
- 修改后**前台立即生效**（本地浏览器）

### 2. 广告位管理
- 左侧切换到「广告位管理」
- 可新增/编辑/删除广告位
- 广告显示在首页顶部导航下方
- 广告位数据保存在本地浏览器

### 3. 云端同步（重要！）
- 修改站点后，必须到「云端同步」页面
- 点击「☁️ 同步到云端（更新线上数据）」
- 数据上传到 jsonbin.io，**所有访客刷新页面即看到最新数据**
- 也可以点击「🔄 从云端重新加载」放弃本地修改，恢复云端数据

---

## 五、修改管理密码

编辑 `lib/adminConfig.ts`：
```typescript
export const ADMIN_CONFIG = {
  password: "你的新密码",  // 改这里
};
```
修改后重新 `npm run build` 并部署。

---

## 六、常见问题

**Q: 忘记 Bin ID 怎么办？**
A: 登录 https://jsonbin.io/app/dashboard，在 Bins 列表中找到名称为 `iosdh-nav-data` 的 Bin，复制其 ID。

**Q: 换了浏览器后台数据不同步？**
A: 站点数据存在 jsonbin 云端，换浏览器用「已有 Bin」模式登录即可看到相同数据。广告位数据存在本地浏览器，换浏览器需要重新配置。

**Q: API Key 泄露了怎么办？**
A: 到 jsonbin Dashboard 重新生成一个新的 API Key，旧的立即失效。

**Q: 构建时详情页生成失败？**
A: 确保 `public/data/sites.json` 存在且格式正确。详情页数量 = 站点数量 + 5。

**Q: 前台数据不更新？**
A: 后台修改后必须点击「同步到云端」。同步后访客需要刷新页面（或强刷 Ctrl+Shift+R）才能看到最新数据。

---

## 七、项目结构

```
iosdh-nav/
├── app/
│   ├── page.tsx              # 首页
│   ├── admin/page.tsx        # 管理后台
│   ├── site/[id]/page.tsx    # 站点详情页（服务端，预生成）
│   ├── layout.tsx             # 布局
│   └── globals.css            # 全局样式
├── components/
│   ├── SiteCard.tsx           # 站点卡片
│   ├── SiteDetail.tsx         # 站点详情（客户端）
│   ├── SearchBox.tsx          # 搜索框
│   └── TopAd.tsx              # 顶部广告位
├── lib/
│   ├── sites.ts               # 类型定义 + 导航分组
│   ├── dataLoader.ts          # 数据加载器（jsonbin 云端）
│   ├── jsonbin.ts             # jsonbin API 封装
│   ├── adminConfig.ts         # 后台配置（管理密码）
│   ├── ad.ts                  # 广告位配置
│   └── categories_part*.ts    # 默认站点数据（兜底）
├── public/
│   └── data/sites.json        # 默认数据 JSON（打包在站点内）
├── .github/workflows/deploy.yml  # GitHub Pages 自动部署
└── next.config.ts             # Next.js 配置（静态导出）
```
